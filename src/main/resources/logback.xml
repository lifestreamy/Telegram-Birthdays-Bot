<?xml version="1.0"?>

<configuration>

    <appender class="ch.qos.logback.core.ConsoleAppender" name="CONSOLE">

        <withJansi>true</withJansi>

        <encoder>

            <pattern>%d [%t] %highlight(%-5level) %cyan(%logger{15}) - %m%n</pattern>

        </encoder>

    </appender>

    <appender class="ch.qos.logback.core.rolling.RollingFileAppender" name="FILE" >
        <file>logs/bot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>logs/bot-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
            <maxHistory>${log_max_history:-7}</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{dd.MM.yyyy HH:mm:ss.SSS} [%thread] %-5level %logger{1}:%line - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="org.jooq" level="INFO"/>

    <logger name="io.ktor" level="ERROR"/>

    <logger name="eu.vendeli.tgbot" level="ERROR"/>

    <logger name="TelegramUpdateHandler" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>





</configuration>